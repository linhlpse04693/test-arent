version: "3"
services:
  postgres:
    image: postgres:15.1
    expose:
      - "5432"
    ports:
      - "5432:5432"
    networks:
      - postgres
    volumes:
      - /data/postgres:/data/postgres
    env_file:
      - docker.env

  nestjs-api:
    container_name: api
    build:
      context: '.'
      dockerfile: .docker/node/Dockerfile
    volumes:
      - .:/usr/src/app
    env_file:
      - .env
    ports:
      - "3000:3000"
    networks:
      - postgres

networks:
  postgres:
    driver: bridge